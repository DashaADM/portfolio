export const COLLECTION_SLUG = {
  PAGES: 'pages',
  PROJECTS: 'projects',
  SERVICES: 'services',
  USERS: 'users',
  MEDIA: 'media',
} as const

export const GLOBAL_SLUG = {
  SETTINGS: 'settings',
} as const
